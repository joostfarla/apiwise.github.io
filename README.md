# Apiwise Website

## Develop

To serve the website from the [Vagrant Devbox](https://bitbucket.org/apiwise/devbox), run the following command:

```
jekyll serve --host vagrant
```

Now you can visit the webserver in your browser:

```
http://vagrant:4000/
```
